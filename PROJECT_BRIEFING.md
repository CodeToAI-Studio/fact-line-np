# Project Briefing — superseded

This file's contents have been folded into **`CLAUDE.md`** in the project root, which is now
the single running log and handoff document (what's done, small changes, what's next, key
decisions, open blockers).

Read `CLAUDE.md` instead. Keeping two documents in sync was a drift risk — and undetected
drift between duplicated copies is a bug this project has already been bitten by twice
(the model list, and the embedding model).
